# slides-to-latex dependencies
# ----------------------------

# Core — required for PPTX extraction and hard-filter image analysis
Pillow>=10.0.0

# Optional — OCR for figure classification (recommended)
#   Also requires the tesseract binary:
#     macOS: brew install tesseract
#     Linux: apt install tesseract-ocr
# pytesseract>=0.3.10

# PDF extraction path
#   Text/table extraction uses pdfplumber.
#   Embedded image extraction also benefits from pdfimages from Poppler:
#     macOS: brew install poppler
#     Linux: apt install poppler-utils
pdfplumber>=0.11.0

# Optional — EMF/WMF conversion during extraction
#   Install ONE of:
#     ImageMagick:  brew install imagemagick   /   apt install imagemagick
#     LibreOffice:  brew install --cask libreoffice
